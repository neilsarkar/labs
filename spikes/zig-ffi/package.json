{
  "name": "zig-ffi",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "ci:tsc": "tsc --noEmit",
    "dev": "bun run --watch src/index.ts",
    "build:wasm": "zig build -p .",
    "check:wasm": "wasm-objdump -x public/sum.wasm"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
