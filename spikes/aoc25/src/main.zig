const std = @import("std");
const advent = @import("advent");

pub fn main() !void {
    // Prints to stderr, ignoring potential errors.
    std.debug.print("Solutions in root.zig, run with zig build test\n");
}
