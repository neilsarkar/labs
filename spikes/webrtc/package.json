{
  "name": "webrtc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "bun run build && fly deploy",
    "run": "docker build -t webrtc . && docker run --rm -p 3000:3000 webrtc"
  },
  "devDependencies": {
    "@types/alpinejs": "^3.13.11",
    "@types/bun": "^1.3.3",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "alpinejs": "^3.15.2",
    "partysocket": "^1.1.6"
  }
}
